# 客户端开发规则

- 客户端只能依赖 `contracts/` 和公开 API，不读取数据库、提示词或后端内部函数作为运行时契约。
- 账户、额度、排盘、公开条件、任务状态和模型选择由后端决定；客户端只展示服务端结果。
- 客户端不得选择或展示供应商、模型、提示词版本、分析路由、raw reasoning、usage 或成本；只展示后端返回的产品内容和语义状态。
- 不在客户端仓库路径中保存生产 key、会话令牌、Webhook、真实用户数据或部署凭据。
- 缺少接口或字段时先更新后端契约，不在客户端使用临时猜测字段绕过。
- 所有客户端都必须覆盖加载、空状态、错误、取消、断线恢复和终态。
- 本仓库不存在私有后端源码；需要跨层变化时，先更新公共契约并明确兼容版本。
- 根目录运行 `npm run check` 和 `npm run package:web`；Android 改动还要运行 `cd android && ./gradlew :app:assembleDebug`。
- 公开仓库 CI 只能构建客户端产物，不能加入服务器 SSH、生产部署或应用商店签名凭据。
